name: nosql_database
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  sembast: ^3.1.1
  path_provider: ^2.0.5
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
